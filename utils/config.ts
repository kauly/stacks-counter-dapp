const config = {
  contractAddress: process.env.NEXT_PUBLIC_CONTRACT_ADDRESS,
  chainAddress: process.env.NEXT_PUBLIC_CHAIN_ADDRESS,
  contractName: process.env.NEXT_PUBLIC_CONTRACT_NAME,
}

export default config
